{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  },
  "engines": {
    "node": "11.4.0"
  },
  "repository": {
    "url": "https://github.com/koko57/musiqueue.git"
  },
  "author": "Koko",
  "license": "ISC",
  "dependencies": {
    "apollo-engine": "^1.1.2",
    "cors": "^2.8.5",
    "dotenv": "^6.2.0",
    "express": "^4.16.4",
    "express-graphql": "^0.7.1",
    "graphql": "^14.0.2",
    "mongoose": "^5.13.7"
  }
}
